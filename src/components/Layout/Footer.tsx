export default function Footer() {
  return (
    <footer className="spatial-glass border-t border-white/[0.06] mt-16">
      <div className="max-w-7xl mx-auto px-4 py-8 text-center text-sm" style={{ color: 'var(--text-secondary)' }}>
        <p>PDF Tools — Herramientas PDF gratis, 100% en tu navegador.</p>
        <p className="mt-2" style={{ color: 'var(--text-tertiary)' }}>Los archivos nunca salen de tu dispositivo.</p>
      </div>
    </footer>
  )
}
